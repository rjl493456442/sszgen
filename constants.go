package main

const (
	BytesPerLengthOffset = 4  // Number of bytes per serialized length offset
	BytesPerChunk        = 32 // Number of bytes per chunk
	BitsPerByte          = 8  // Number of bits per byte
)
